- project:
    check:
      jobs:
        - "thoth-coala"
    gate:
      queue: "thoth-station/core"
      jobs:
        - "thoth-coala"
        - "thoth-pytest"
    post:
      jobs:
        - "trigger-build":
            vars:
              cluster: "paas.psi.redhat.com"
              namespace: "thoth-test-core"
              buildConfigName: "unresolved-package-handler"

    kebechet-auto-gate:
      queue: "thoth-station/core"
      jobs:
        - "thoth-coala"
